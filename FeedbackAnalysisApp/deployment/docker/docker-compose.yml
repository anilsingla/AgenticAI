services:
  app:
    build:
      context: ../..
      dockerfile: deployment/docker/Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ../../data:/app/data
    env_file:
      - ../../.env
    restart: unless-stopped
